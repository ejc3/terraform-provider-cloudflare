---
page_title: "cloudflare_workers_build_repository_connection Resource - Cloudflare"
subcategory: ""
description: |-
  Accepted Permissions
  Workers CI Write
  Manages a typed Workers Builds repository connection. Cloudflare exposes no read or list endpoint for this object, so refresh preserves the last confirmed state and import is intentionally unsupported. For private GitHub repositories, install and authorize the Cloudflare Workers and Pages GitHub App before creating the connection.
---

# cloudflare_workers_build_repository_connection (Resource)

Accepted Permissions

- `Workers CI Write`


Manages a typed Workers Builds repository connection. Cloudflare exposes no read or list endpoint for this object, so refresh preserves the last confirmed state and import is intentionally unsupported. For private GitHub repositories, install and authorize the Cloudflare Workers and Pages GitHub App before creating the connection.

## Example Usage

```terraform
resource "cloudflare_workers_build_repository_connection" "colton_games" {
  account_id            = "12ea67fb7ced068de03f35c22688e436"
  provider_type         = "github"
  provider_account_id   = "250920182"
  provider_account_name = "CoderColton"
  repo_id               = "1120877379"
  repo_name             = "colton-games"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `account_id` (String) Cloudflare account identifier.
- `provider_account_id` (String) Provider account numeric identifier, represented as a string to preserve precision.
- `provider_account_name` (String) Provider account name.
- `provider_type` (String) Git provider type.
- `repo_id` (String) Repository numeric identifier, represented as a string to preserve precision.
- `repo_name` (String) Repository name.

### Read-Only

- `id` (String) Repository connection UUID.

## Import


~> This resource does not currently support `terraform import`.
